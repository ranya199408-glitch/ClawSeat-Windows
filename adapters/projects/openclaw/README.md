## OpenClaw Adapter

This directory is reserved for the OpenClaw consumer adapter.

Like `adapters/projects/cartooner/`, it should eventually contain:

- OpenClaw-specific profiles
- wrapper skills
- adapter scripts
- any consumer-facing template overrides

The OpenClaw product source tree stays outside ClawSeat core.
