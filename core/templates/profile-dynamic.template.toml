version = 1
profile_name = "{{project}}"
template_name = "gstack-harness"
project_name = "{{project}}"
repo_root = "{{repo_root}}"
profile_path = "{{profile_path}}"
tasks_root = "{{tasks_root}}"
project_doc = "{{project_doc}}"
tasks_doc = "{{tasks_doc}}"
status_doc = "{{status_doc}}"
send_script = "{{send_script}}"
status_script = ""
patrol_script = ""
agent_admin = "{{agent_admin}}"
workspace_root = "{{workspace_root}}"
handoff_dir = "{{handoff_dir}}"
heartbeat_owner = ""
heartbeat_transport = "tmux"
active_loop_owner = "memory"
default_notify_target = "memory"
heartbeat_receipt = "{{heartbeat_receipt}}"
seats = {{seats}}
heartbeat_seats = []

[seat_roles]
{{seat_roles_block}}

[dynamic_roster]
enabled = true
session_root = "{{session_root}}"
materialized_seats = {{seats}}
runtime_seats = {{seats}}
bootstrap_seats = []
default_start_seats = {{seats}}
compat_legacy_seats = false

{{seat_overrides_block}}
