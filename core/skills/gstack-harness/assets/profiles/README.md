Project-specific profiles are intentionally not part of the ClawSeat core skill.

Put consumer/sample profiles under:

- `adapters/<project>/`
- `examples/<project>/profiles/`

The core harness should stay reusable and consumer-agnostic.
